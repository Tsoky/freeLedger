<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_wallet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_wallet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ManagePlansActivity"
            android:exported="false" />

        <activity
            android:name=".ManageAccountsActivity"
            android:exported="false" />

        <activity
            android:name=".ManageCategoriesActivity"
            android:exported="false" />

        <activity
            android:name=".QuickEntryActivity"
            android:exported="true"
            android:launchMode="singleTop" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PaymentNotificationListener"
            android:exported="true"
            android:enabled="true"
            android:directBootAware="true"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse"
            android:label="支付检测"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="notification_listener_monitor" />
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".IgnoreReceiver"
            android:exported="false" />

    </application>
</manifest>
